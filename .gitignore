# Dependencies
node_modules/
server/node_modules/
client/node_modules/

# Build outputs
server/dist/
client/dist/

# Database (contains your data — back up separately)
data/
*.db
*.db-shm
*.db-wal

# Credentials — never commit these
.env
*.swp
*.swo

# Claude Code local workspace files (machine-specific paths + permissions)
.claude/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Local AI models (GGUF files are large — download via npm run download-model)
models/
*.gguf

# Docker
.dockerignore
